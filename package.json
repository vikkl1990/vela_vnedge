{
  "name": "vela-vnedge",
  "version": "0.1.0",
  "private": true,
  "description": "VNEdge — Pine-script scanner bot (PineTS + Vela) paper-trading on Delta Exchange India",
  "workspaces": ["server", "dashboard"],
  "scripts": {
    "dev": "npm run dev --workspace=server",
    "dev:dashboard": "npm run dev --workspace=dashboard",
    "build": "npm run build --workspace=dashboard",
    "start": "npm run start --workspace=server",
    "test": "npm test --workspace=server",
    "backtest": "npm run backtest --workspace=server"
  },
  "engines": { "node": ">=22.18" }
}
