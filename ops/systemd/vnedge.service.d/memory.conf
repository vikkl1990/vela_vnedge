# Keep the VM reachable whatever a script does: past this the kernel stops the bot (systemd then
# restarts it) instead of the whole machine thrashing until SSH stops answering (2026-09-22).
[Service]
MemoryHigh=8G
MemoryMax=10G
MemorySwapMax=0
